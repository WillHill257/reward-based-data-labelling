import { shallowMount } from "@vue/test-utils";
import UploadImage from "@/components/CreateJob/components/UploadImage.vue";
import Vue from "vue";
import Vuetify from "vuetify";

Vue.use(Vuetify);

describe("when upload image component is loaded", () => {
  // const wrapper = shallowMount(UploadImage);
  test.todo("should have an drag and drop area for file uploading");
  test.todo("should store the files uploaded");
});
